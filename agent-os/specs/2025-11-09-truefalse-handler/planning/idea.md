# Raw Idea: TrueFalse Handler

Create handlers for H5P.TrueFalse content type (standalone and embedded). H5P.TrueFalse is a simple true/false question content type where users answer whether a statement is true or false.

Following the Handler Development Guide patterns:
- Standalone-first architecture (TrueFalse is runnable)
- Manual handler (TrueFalseHandler) for YAML/JSON input
- AI handler (AITrueFalseHandler) for AI-generated content
- AIPromptBuilder integration with Universal AI Configuration
- Comprehensive validation and testing

Create the spec folder structure at: agent-os/specs/truefalse-handler/

Save the raw idea to planning/idea.md
